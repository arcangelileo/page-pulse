# =============================================================================
# PagePulse Configuration
# =============================================================================
# Copy this file to .env and update the values below.
# All variables have sensible defaults for local development.
#
#   cp .env.example .env

# -- Application --------------------------------------------------------------
APP_NAME=PagePulse                         # Display name (shown in UI header)
APP_ENV=development                        # Environment: development | production
DEBUG=true                                 # Enable debug mode (disable in production)

# -- Security -----------------------------------------------------------------
# IMPORTANT: Generate unique random values for production!
#   python3 -c "import secrets; print(secrets.token_urlsafe(32))"
SECRET_KEY=change-me-to-a-random-secret-key       # Daily visitor-hash salt
JWT_SECRET_KEY=change-me-to-a-random-jwt-secret    # JWT token signing key

# -- JWT Authentication -------------------------------------------------------
JWT_ALGORITHM=HS256                        # Token signing algorithm
JWT_ACCESS_TOKEN_EXPIRE_MINUTES=1440       # Token TTL in minutes (default: 24h)

# -- Database -----------------------------------------------------------------
# SQLite (default — great for single-server deployments up to ~100K views/day):
DATABASE_URL=sqlite+aiosqlite:///./pagepulse.db
# PostgreSQL (for high-traffic or multi-server deployments):
# DATABASE_URL=postgresql+asyncpg://user:password@localhost:5432/pagepulse

# -- Server -------------------------------------------------------------------
HOST=0.0.0.0                               # Bind address
PORT=8000                                  # Bind port
